
#lang liso

filt[pred?, xs] =
   if null?[xs]:
      []
      block:
         x = car[xs]
         rest = filt[pred?, cdr[xs]]
         if pred?[x]:
            x :: rest
            rest

results = filt[[x] -> x > 0
               [0, 1, -2, -3, 4]]
min results

