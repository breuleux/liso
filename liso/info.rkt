
#lang setup/infotab

(define name "LISO")

(define blurb
  '("An alternative syntax for s-expression languages entirely "
    "based on an operator precedence table."))

(define release-notes
  '("Some minimal documentation."))

(define version "0.0")

(define categories
  '(devtools metaprogramming))

(define homepage
  "https://github.com/breuleux/liso")

(define repositories
  '("4.x"))

(define scribblings
  '(("liso.scrbl")))
